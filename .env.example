# Copy this file to `.env` and fill in the value below.
# The Postgres URI must include the username, password, host, port, and
# database name. The Supabase pooler URI works here for the Render deploy.
POSTGRES_URI=postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
